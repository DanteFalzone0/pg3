typedef struct {
    int x;
    int y;
} CoordPair; // basically the same as the CoordPair type in djf-3d

typedef struct {
    float x_vel;
    float y_vel;
} WindVector; // the wind direction for a given AirCell
